<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- SMS permissions -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <!-- Required for sending SMS if your app were to send replies or forwards directly -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Background service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required for data sync foreground service type on Android 10+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Notification permission for Android 13+ (API 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Auto-start permission for BootReceiver -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OTPRelay"
        tools:targetApi="31"
        tools:replace="android:allowBackup,android:dataExtractionRules,android:fullBackupContent,android:icon,android:label,android:roundIcon,android:supportsRtl,android:theme">

        <!-- Declare the default SMS app component -->
        <!-- This meta-data entry is specifically looked for by some OEMs -->
        <meta-data android:name="android.telephony.DEFAULT_SMS_APP"
            android:value="com.example.otprelay.MainActivity" />

        <!-- Main Activity: Entry point for the user interface -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.OTPRelay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- Explicitly declare as a messaging app category -->
                <category android:name="android.intent.category.APP_MESSAGING" />
            </intent-filter>
            <!-- Intent filter to make MainActivity capable of handling SENDTO for SMS/MMS -->
            <!-- This is crucial for being a default SMS app -->
            <intent-filter>
                <action android:name="android.intent.action.SENDTO" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </activity>

        <!-- SMS Test Activity: For debugging SMS access and parsing -->
        <activity
            android:name=".SmsTestActivity"
            android:exported="false"
            android:theme="@style/Theme.OTPRelay" />

        <!-- Background OTP Service: The core persistent service for SMS monitoring -->
        <service
            android:name=".OTPService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- SMS Receiver: Listens for incoming SMS broadcasts -->
        <receiver
            android:name=".SMSReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true"
            android:permission="android.permission.BROADCAST_SMS"> <!-- NEW: Added BROADCAST_SMS permission -->
            <!-- Action for receiving SMS (already present) -->
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
            <!-- REQUIRED for being a default SMS app - receives SMS messages -->
            <intent-filter android:priority="2147483647"> <!-- Set a high priority to ensure it's preferred by the system -->
                <action android:name="android.provider.Telephony.SMS_DELIVER" />
                <data android:scheme="sms" />
                <data android:mimeType="vnd.android-dir/mms-sms" />
            </intent-filter>
            <!-- REQUIRED for being a default SMS app - receives WAP Push messages (MMS) -->
            <intent-filter android:priority="2147483647"> <!-- Max priority -->
                <action android:name="android.provider.Telephony.WAP_PUSH_DELIVER" />
                <data android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>
        </receiver>

        <!-- Boot Receiver: Starts the OTPService on device boot -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Notification Listener Service: Fallback for aggressive OEM optimizations -->
        <service
            android:name=".OTPNotificationListener"
            android:label="OTP Notification Reader"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>
